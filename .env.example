# --- SmartChat Core ---
# Docker compose defaults to using the internal Postgres container (host: db)
DATABASE_URL=postgresql+asyncpg://smartchat:smartchat_dev@db:5432/smartchat

# Dev convenience: set a static API key for the default tenant (leave empty in production)
DEFAULT_API_KEY=test-key-123

# --- LLM Provider ---
# OpenAI
OPENAI_API_KEY=sk-your-key-here

# Optional: Ollama / OpenAI-compatible base URL
# (Also configurable per-tenant via Admin → LLM Config)
# OLLAMA_BASE_URL=http://localhost:11434/v1
